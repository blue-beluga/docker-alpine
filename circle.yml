# encoding: UTF-8
---
machine:
  services:
    - docker

dependencies:
  pre:
    - make deps
  override:
    - make build

test:
  override:
    - make test

deployment:
  master:
    branch: master
    commands:
      - make push
      - make publish

general:
  branches:
    ignore:
      - gh-pages
